"""Pluggeasy enums — placeholder.

No code→meaning mappings are documented in the source YAML for fields such as
``working_mode``, ``actual_working_mode``, ``defrost_status``, or
``communication_error``.  These are therefore modelled as plain ``integer``
fields.  Add enum members here once an authoritative mapping is available.
"""
